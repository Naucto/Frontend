FROM node:22-alpine
WORKDIR /app

COPY ./patches ./patches
COPY package.json ./
COPY package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# `--` is required, otherwise npm swallows --host instead of forwarding it to vite.
CMD ["npm", "run", "dev", "--", "--host"]

