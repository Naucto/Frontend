
type SpriteSheet = {
  spriteSheet: string;
  spriteSize: {
    width: number;
    height: number;
  };
  size: {
    width: number;
    height: number;
  };
  stride: number;
}

export type { SpriteSheet };
