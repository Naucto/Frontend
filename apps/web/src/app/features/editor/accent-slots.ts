/**
 * Palette slots the editor offers when something in a project may be given a colour.
 *
 * Slots and not colours: what they paint is the game's own palette, which the project edits, so
 * repainting a slot repaints everything wearing it.
 */
export const ACCENT_SLOTS = [4, 11, 3, 6, 13, 7] as const;
